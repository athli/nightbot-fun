// Note: code is same for its, can't alias, not sure why
// being annoying YEP

$(eval 
    var a = "$(1)".toLowerCase(); 
    switch(a) {
        case "not":
            "yes it is";
            break;
        default:
            "no it isn't";
    }
)